---
phase: 01-difficulty-ladder-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - DIFF-01
  - DIFF-02
user_setup: []
must_haves:
  truths:
    - "The menu only accepts `novice`, `easy`, `normal`, and `hard` as valid difficulty values."
    - "With no stored preferences, `duel` defaults to `novice` and `deathmatch` defaults to `easy`."
    - "`tinyToonDuel_aiDifficultyByMode` remains the only persisted storage entry for per-mode difficulty."
  artifacts:
    - "index.html defines canonical difficulty metadata in `CONFIG.difficulty`."
    - "index.html contains shared normalization and accessor helpers for mode-scoped defaults and persistence."
  key_links:
    - "`CONFIG.difficulty.order`, `labels`, and `byMode` are the only sources for valid options, display names, and per-mode defaults."
    - "`normalizeDifficultyMap()` and `getDifficultyForMode()` share the same fallback source from `CONFIG.difficulty.byMode`."
    - "`setDifficultyForMode()` is the only function allowed to write `tinyToonDuel_aiDifficultyByMode`."
---

<objective>
Lock Phase 1's configuration and persistence foundation so difficulty metadata, default values, and storage compatibility all resolve through one path.

Purpose: Prevent default drift, invalid value leaks, and legacy storage regressions before UI-specific work starts.
Output: A stable `index.html` difficulty core that execute-phase can build on without pulling in Phase 2 rules or Phase 3 balance work.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-difficulty-ladder-persistence/01-CONTEXT.md
@.planning/phases/01-difficulty-ladder-persistence/01-RESEARCH.md
@.planning/phases/01-difficulty-ladder-persistence/01-VALIDATION.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate canonical difficulty metadata</name>
  <files>index.html</files>
  <action>Audit the existing difficulty configuration and keep `CONFIG.difficulty.order`, `CONFIG.difficulty.labels`, and `CONFIG.difficulty.byMode` as the only canonical sources for allowed values, Chinese labels, and per-mode defaults. Remove or refactor any duplicate fallback constants that could cause `duel` or `deathmatch` to drift away from `novice` and `easy`. Do not expand into `modeOverrides`, `getEffectiveGameMode()`, or Phase 2 rule-derivation behavior except to avoid accidental regression.</action>
  <verify>Run the `Quick run command` from `01-VALIDATION.md` and confirm the static checks still detect ordered buttons, labels, defaults, and helper presence.</verify>
  <done>`index.html` exposes exactly one authoritative metadata source for valid difficulties, display labels, and default per-mode values.</done>
</task>

<task type="auto">
  <name>Task 2: Harden normalization and storage access boundaries</name>
  <files>index.html</files>
  <action>Ensure `sanitizeDifficulty()`, `normalizeDifficultyMap()`, `getDifficultyForMode()`, and `setDifficultyForMode()` have non-overlapping responsibilities: sanitize individual values, normalize entire mode maps, read effective per-mode difficulty, and perform the sole persistence write. Keep legacy `easy` / `normal` / `hard` storage compatible, ignore unknown keys, and preserve silent fallback for invalid values. Do not introduce a new migration layer, extra storage keys, or user-facing recovery messages.</action>
  <verify>Run the `Full suite command` from `01-VALIDATION.md`, then manually test the two storage scenarios documented there: no stored map and legacy `{\"duel\":\"easy\",\"deathmatch\":\"normal\"}`.</verify>
  <done>Stored difficulty maps normalize through shared helpers, legacy values load without cleanup, and all writes remain funneled through `tinyToonDuel_aiDifficultyByMode`.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run the `Quick run command` from `01-VALIDATION.md`
- [ ] Run the `Full suite command` from `01-VALIDATION.md`
- [ ] Confirm no-storage defaults resolve to `duel=novice` and `deathmatch=easy`
- [ ] Confirm legacy `easy` / `normal` / `hard` storage still restores both modes without manual cleanup
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 defaults, allowed values, and storage compatibility are driven by a single shared metadata path
- No Phase 2 rule-sync behavior is introduced as a new deliverable
  </success_criteria>

<output>
After completion, create `.planning/phases/01-difficulty-ladder-persistence/01-01-SUMMARY.md`
</output>
