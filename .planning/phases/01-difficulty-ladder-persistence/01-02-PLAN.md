---
phase: 01-difficulty-ladder-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - index.html
autonomous: true
requirements:
  - DIFF-01
  - DIFF-02
user_setup: []
must_haves:
  truths:
    - "The difficulty buttons always appear in `novice -> easy -> normal -> hard` order."
    - "The visible labels stay in Chinese as `新手 / 简单 / 普通 / 困难`."
    - "Switching between `duel` and `deathmatch` restores each mode's saved difficulty without overwriting the other mode."
  artifacts:
    - "index.html binds difficulty button clicks through a single mode-scoped write path."
    - "index.html updates menu labels, button highlights, and mode copy from the currently selected mode's saved difficulty."
  key_links:
    - "`setupDifficultySelect()` delegates writes to `setDifficultyForMode()` instead of touching storage directly."
    - "`setGameMode()` and `updateDifficultyUI()` both read the active mode's stored difficulty through shared accessors."
    - "Menu copy and button highlight behavior reflect saved per-mode difficulty without adding new helper panels or recommendation badges."
---

<objective>
Finish the Phase 1 menu behavior so button order, Chinese labels, and per-mode memory stay consistent when the player switches modes or revisits the menu.

Purpose: Make the new defaults and persistence behavior visible and reliable without pulling Phase 2 rule-sync into the current phase.
Output: A menu flow in `index.html` where difficulty buttons, labels, and mode switching all honor the same mode-scoped saved value.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-difficulty-ladder-persistence/01-CONTEXT.md
@.planning/phases/01-difficulty-ladder-persistence/01-RESEARCH.md
@.planning/phases/01-difficulty-ladder-persistence/01-VALIDATION.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route menu interaction through shared per-mode persistence</name>
  <files>index.html</files>
  <action>Review `setupDifficultySelect()`, `setGameMode()`, and related menu event wiring so every click updates only the current mode through `setDifficultyForMode()`. Eliminate any direct UI-only state changes that could bypass storage normalization or overwrite the other mode's saved choice. Keep the menu lightweight: no badges, helper cards, or new explanatory UI.</action>
  <verify>Run the `Quick run command` from `01-VALIDATION.md`, then switch between `duel` and `deathmatch` in the browser and confirm each mode keeps its own saved selection.</verify>
  <done>Menu event wiring updates difficulty through one shared write path, and switching modes no longer risks losing the other mode's remembered difficulty.</done>
</task>

<task type="auto">
  <name>Task 2: Stabilize labels, button order, and silent fallback behavior in the menu</name>
  <files>index.html</files>
  <action>Ensure `updateDifficultyUI()` and any menu-copy helpers always read the current mode's saved difficulty via shared accessors, render Chinese labels, preserve fixed button order, and silently recover from invalid stored values. Keep Phase 1 scoped to labels/highlights/memory consistency; do not make Phase 2 claims about rule-derived player counts or lives in this plan.</action>
  <verify>Run the `Full suite command` from `01-VALIDATION.md`, then execute the manual matrix for invalid storage fallback, button order, Chinese labels, and cross-mode memory.</verify>
  <done>The menu consistently shows `新手 / 简单 / 普通 / 困难`, restores the right saved difficulty after mode switches, and silently falls back from invalid storage without extra UI noise.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run the `Quick run command` from `01-VALIDATION.md`
- [ ] Run the `Full suite command` from `01-VALIDATION.md`
- [ ] Confirm button order stays `novice -> easy -> normal -> hard`
- [ ] Confirm labels stay `新手 / 简单 / 普通 / 困难`
- [ ] Confirm mode switching restores each mode's saved difficulty and invalid storage falls back silently
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Menu button order, Chinese labels, and per-mode memory are stable and user-visible
- Phase 2 rule-sync work remains out of scope for this plan
  </success_criteria>

<output>
After completion, create `.planning/phases/01-difficulty-ladder-persistence/01-02-SUMMARY.md`
</output>
