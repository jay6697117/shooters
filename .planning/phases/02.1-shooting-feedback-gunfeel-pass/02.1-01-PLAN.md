---
phase: 02.1-shooting-feedback-gunfeel-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - GF-01
user_setup: []
must_haves:
  truths:
    - "Every trigger pull resolves to exactly one primary outcome: `kill`, `player_hit`, `environment_hit`, or `miss`."
    - "`shotgun` pellets aggregate into one trigger-level result instead of replaying primary feedback per pellet."
    - "AI bullets still use the same damage / kill pipeline, but they never trigger the player's shooter-side HUD confirm."
  artifacts:
    - "index.html separates shot resolution, damage commit, and feedback presentation through local helpers."
    - "index.html routes kill promotion through the return value of `applyDamage()`."
  key_links:
    - "`attemptFire()` builds a trigger-level `shotResult` and calls `resolveShotPellet()`, `commitShotPellet()`, and `finalizeShotResult()`."
    - "`presentShotFeedback()` only consumes the final aggregated outcome."
---

<objective>
Land the Phase 2.1 outcome seam so the shooting loop has one authoritative trigger-level result before any HUD or camera feedback is presented.

Purpose: Eliminate ambiguous per-pellet side effects and make later gunfeel tuning operate on a stable shot outcome contract.
Output: `index.html` exposes a clear `resolve -> commit -> present` path around `attemptFire()`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-shooting-feedback-gunfeel-pass/02.1-CONTEXT.md
@.planning/phases/02.1-shooting-feedback-gunfeel-pass/02.1-RESEARCH.md
@.planning/phases/02.1-shooting-feedback-gunfeel-pass/02.1-VALIDATION.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce trigger-level shot result aggregation</name>
  <files>index.html</files>
  <action>Keep the implementation in the existing monolith, but split the current fire path into local helpers that resolve pellet hits, commit damage / environment state, and then finalize one primary outcome for the whole trigger pull. Preserve the fixed priority `kill > player_hit > environment_hit > miss` and ensure `shotgun` still consumes one ammo per trigger while producing only one primary confirm candidate.</action>
  <verify>Run the `Quick run command` from `02.1-VALIDATION.md` and confirm the static checks find the helper seam around `attemptFire()`.</verify>
  <done>The fire loop no longer mixes pellet hit detection and HUD presentation in one branch, and trigger-level outcome promotion is explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Route AI and player shots through the same damage seam without shared HUD side effects</name>
  <files>index.html</files>
  <action>Make `applyDamage()` return kill promotion information so player and AI shots can share the same damage pipeline, but keep shooter-side confirm scoped to the local player only. Near-miss should continue to work for true misses, including AI shots that narrowly miss the player.</action>
  <verify>Run the `Full suite command` from `02.1-VALIDATION.md`, then inspect the browser state hook to confirm player-fired outcomes are tracked without AI HUD confirms.</verify>
  <done>Damage, kill promotion, and miss routing are shared, while HUD confirms stay player-only.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run the `Quick run command` from `02.1-VALIDATION.md`
- [ ] Run the `Full suite command` from `02.1-VALIDATION.md`
- [ ] Confirm `attemptFire()` produces one trigger-level primary outcome
- [ ] Confirm AI shots never trigger player shooter-side confirm
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No syntax or formatting errors introduced
- Trigger-level shot results are the sole source for Phase 2.1 primary feedback decisions
</success_criteria>
