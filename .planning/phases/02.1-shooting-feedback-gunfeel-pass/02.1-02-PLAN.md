---
phase: 02.1-shooting-feedback-gunfeel-pass
plan: 02
type: execute
wave: 2
depends_on:
  - "02.1-01"
files_modified:
  - index.html
autonomous: true
requirements:
  - GF-01
  - GF-02
user_setup: []
must_haves:
  truths:
    - "The HUD center confirm is the primary layer for shooter-side clarity."
    - "`kill` feedback is visibly stronger than `player_hit`, and `environment_hit` is clearly weaker than both."
    - "`miss` never shows the primary center confirm."
  artifacts:
    - "index.html contains a dedicated center confirm overlay and update loop."
    - "Shot feedback reset paths clear stale state during menu / round / match transitions."
  key_links:
    - "`presentShotFeedback()` owns outcome-to-feedback routing."
    - "`checkNearMiss()` only runs for true miss outcomes."
---

<objective>
Ship the primary HUD confirm layer and the priority rules that distinguish `player_hit`, `kill`, `environment_hit`, and `miss`.

Purpose: Make the player immediately understand whether a shot connected, killed, or only hit the environment, without relying on killcam or full-screen noise.
Output: The HUD, camera pulse, hit flash, and near-miss effects all route from the finalized shot outcome with explicit reset behavior.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-shooting-feedback-gunfeel-pass/02.1-CONTEXT.md
@.planning/phases/02.1-shooting-feedback-gunfeel-pass/02.1-RESEARCH.md
@.planning/phases/02.1-shooting-feedback-gunfeel-pass/02.1-VALIDATION.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the center confirm layer and explicit routing rules</name>
  <files>index.html</files>
  <action>Create a dedicated center confirm overlay with a tiny per-frame update loop, then route `kill`, `player_hit`, and `environment_hit` into distinct glyph / label / opacity states. Keep `miss` out of that layer entirely and preserve `near-miss` as the only dramatic miss branch.</action>
  <verify>Run the `Quick run command` from `02.1-VALIDATION.md`, then open the browser and confirm the overlay renders without console errors.</verify>
  <done>The center confirm exists as a primary layer and only non-miss outcomes can activate it.</done>
</task>

<task type="auto">
  <name>Task 2: Harden reset paths and kill priority transitions</name>
  <files>index.html</files>
  <action>Ensure menu resets, round resets, match restarts, and killcam transitions do not leave stale confirm state behind. Keep kill feedback strong enough to read before or during killcam entry, but do not let reloads, weapon switches, or dead attackers create false `player_hit` confirms.</action>
  <verify>Run the `Full suite command` from `02.1-VALIDATION.md`, then manually cover round reset, match restart, and killcam entry / exit in the browser.</verify>
  <done>Transient feedback state is cleaned up on the documented reset paths, and kill confirm remains readable before cinematic cleanup takes over.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run the `Quick run command` from `02.1-VALIDATION.md`
- [ ] Run the `Full suite command` from `02.1-VALIDATION.md`
- [ ] Confirm `kill` reads stronger than `player_hit`
- [ ] Confirm `environment_hit` does not impersonate `player_hit`
- [ ] Confirm reset paths do not leave stale feedback on screen
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No syntax or formatting errors introduced
- Primary feedback routing follows `kill > player_hit > environment_hit > miss`
</success_criteria>
