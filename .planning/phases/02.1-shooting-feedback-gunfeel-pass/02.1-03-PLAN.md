---
phase: 02.1-shooting-feedback-gunfeel-pass
plan: 03
type: execute
wave: 3
depends_on:
  - "02.1-02"
files_modified:
  - index.html
  - progress.md
autonomous: true
requirements:
  - GF-01
  - GF-02
  - GF-03
user_setup: []
must_haves:
  truths:
    - "`pistol`, `smg`, and `shotgun` have distinct feedback baselines instead of one shared template."
    - "`deathmatch` is more restrained than `duel`, especially for `smg` cadence."
    - "Validation remains manual-first; no new test framework is introduced."
  artifacts:
    - "index.html stores feedback tuning in one `weaponFeedbackTuning` table keyed by mode and weapon."
    - "Phase progress notes capture implementation and validation outcomes for Phase 2.1."
  key_links:
    - "`getWeaponFeedbackProfile()` is the only read path for mode-aware feedback tuning."
    - "Browser validation uses the built-in phase test hooks instead of a new harness."
---

<objective>
Finish the baseline weapon identity pass and verify it in both `duel` and `deathmatch` without expanding into a full combat rebalance.

Purpose: Give each gun a clear feedback personality now, while leaving Phase 3 room for numeric polish instead of structural rework.
Output: A centralized tuning table, stable verification hooks, and updated execution notes for the current phase.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-shooting-feedback-gunfeel-pass/02.1-CONTEXT.md
@.planning/phases/02.1-shooting-feedback-gunfeel-pass/02.1-RESEARCH.md
@.planning/phases/02.1-shooting-feedback-gunfeel-pass/02.1-VALIDATION.md
@progress.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize weapon x mode feedback tuning</name>
  <files>index.html</files>
  <action>Keep all confirm timing, opacity, pulse, shake, and throttle values in one `weaponFeedbackTuning` table keyed by `duel` / `deathmatch` and weapon id. Make `pistol` the crisp baseline, `smg` the light high-cadence baseline with stronger throttling, and `shotgun` the heavy single-burst baseline.</action>
  <verify>Run the `Full suite command` from `02.1-VALIDATION.md`, then use the browser hook to inspect per-weapon outcome labels and confirm the tuning table is the only read path.</verify>
  <done>Weapon identity is centralized and mode-aware without scattering feedback constants across the file.</done>
</task>

<task type="auto">
  <name>Task 2: Record Phase 2.1 verification evidence and implementation notes</name>
  <files>progress.md</files>
  <action>Append a Phase 2.1 entry to `progress.md` covering the outcome seam, center confirm layer, weapon identity tuning, built-in browser test hooks, and the final verification evidence collected during implementation.</action>
  <verify>Manually review the new progress entry and ensure it matches the actual code and verification outcomes.</verify>
  <done>The current session leaves an accurate implementation trail for the inserted gunfeel phase.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run the `Full suite command` from `02.1-VALIDATION.md`
- [ ] Confirm each weapon has a distinct baseline feedback profile
- [ ] Confirm `deathmatch` is more restrained than `duel`, especially for `smg`
- [ ] Update `progress.md` with implementation and verification evidence
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No syntax or formatting errors introduced
- Phase 2.1 ends with centralized tuning, reproducible browser verification, and updated progress notes
</success_criteria>
