---
phase: 2.1
slug: shooting-feedback-gunfeel-pass
status: verified
nyquist_compliant: true
wave_0_complete: true
created: 2026-03-09
---

# Phase 2.1 - Validation Strategy

> Manual-first validation contract for gunfeel and shot feedback changes.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | other - inline static smoke + browser gameplay verification |
| **Config file** | none - phase stays inside existing single-file runtime |
| **Quick run command** | `node -e "const fs=require('fs');const html=fs.readFileSync('index.html','utf8');const checks=[/id=\\\"shotConfirm\\\"/,/weaponFeedbackTuning/,/function resolveShotPellet\\(/,/function finalizeShotResult\\(/,/function presentShotFeedback\\(/,/window\\.phase21TestApi = createPhase21TestApi\\(\\)/]; if(!checks.every((re)=>re.test(html))) process.exit(1);"` |
| **Full suite command** | `node -e "const fs=require('fs');const html=fs.readFileSync('index.html','utf8');const checks=[/id=\\\"shotConfirm\\\"/,/weaponFeedbackTuning/,/function resolveShotPellet\\(/,/function finalizeShotResult\\(/,/function presentShotFeedback\\(/,/function updateShotFeedbackUI\\(/,/lastShotFeedback/,/shotFeedbackEventCount/,/setEntityPosition\\(/,/zeroSpread/]; if(!checks.every((re)=>re.test(html))) process.exit(1);"` |
| **Estimated runtime** | ~3 seconds |

---

## Sampling Rate

- **After every task commit:** Run `Quick run command`
- **After every plan wave:** Run `Full suite command`
- **Before final completion claim:** Browser open + console error check + manual gameplay matrix
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 02.1-01-01 | 01 | 1 | GF-01 | static smoke | `Quick run command` | ✅ | ✅ green |
| 02.1-02-01 | 02 | 2 | GF-01, GF-02 | static smoke | `Full suite command` | ✅ | ✅ green |
| 02.1-03-01 | 03 | 3 | GF-03 | manual + deterministic browser matrix | `phase21TestApi runtime matrix` | ✅ | ✅ green |
| 02.1-03-02 | 03 | 3 | GF-01, GF-02, GF-03 | browser console | `playwright console error check` | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing infrastructure covers all phase requirements.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `duel` player hit confirm is clear | GF-01 | Needs real-time visual judgment | Start `duel`, fire each weapon at AI, confirm center feedback appears immediately on player hits and is distinct from miss |
| kill confirm is stronger than normal hit | GF-02 | Relative intensity is experiential | Land a killing shot in `duel`, confirm center feedback is visibly stronger and survives into any killcam transition |
| `environment_hit` does not impersonate `player_hit` | GF-01 | Requires comparing visual language | Shoot crates / cover / barrels and verify muted confirm appears, but it is weaker and visually distinct from player hit |
| `smg` remains readable in `deathmatch` | GF-03 | High-frequency readability is perceptual | Enter `deathmatch`, hold fire with `smg`, confirm center HUD does not become noisy or opaque |
| `shotgun` aggregates to one primary confirm | GF-03 | Needs observing one trigger with multiple pellets | Fire `shotgun` at close range and confirm one main center confirm per trigger, not one per pellet |
| `near-miss` only happens on true miss | GF-01 | Requires behavior sequencing | Fire shots that narrowly miss and then direct hits; confirm only miss cases trigger the dramatic near-miss effect |
| reset paths clear stale confirm state | GF-01, GF-02 | Requires flow transitions | Test reload, weapon switch, round reset, pause, killcam exit, and rematch; confirm no stale confirm overlay persists |

---

## Runtime Verification Evidence

**Executed:** 2026-03-09

- Static checks
  - `Quick run command`: pass
  - `Full suite command`: pass
  - `git diff --check`: pass
  - stripped-module syntax smoke: pass

- Browser runtime matrix via `window.phase21TestApi`
  - Stable hit lane in `duel`: `p1=(-10,-7)`, `p2=(-10,-3)`
  - `pistol` hit:
    - outcome `player_hit`
    - `shotConfirm.kind === "player_hit"`
    - `shotFeedbackEventCount === 1`
  - `pistol` kill:
    - set `p2.hp = 16`
    - immediate outcome `kill`
    - `shotConfirm.kind === "kill"`
    - confirm persists into the match-ending `killCam` state on the final-round kill
  - `barrel` / `cover` hit:
    - outcome `environment_hit`
    - `shotConfirm.kind === "environment_hit"`
    - label remained the muted `CLANG` path, distinct from `HIT` / `K.O.`
  - `near-miss`:
    - stable lane `p1=(-6,-7)`, `p2=(-10,-3)`
    - aim point `(-9.5050, -2.5050)`
    - outcome `miss`
    - `lastNearMiss.targetTeam === "p2"`
    - `shotConfirm === null`
    - `shotFeedbackEventCount === 0`
  - `shotgun`:
    - stable lane reused from direct hit
    - single trigger produced one primary confirm event only
    - `shotFeedbackEventCount === 1`
  - `smg` `deathmatch` throttle:
    - stable lane reused from direct hit
    - 4 rapid trigger calls reduced `p2.hp` from `65 -> 33`
    - `shotFeedbackEventCount` stayed at `1` across all four snapshots, confirming throttle suppressed repeated primary confirms

- Reset paths
  - post-kill round reset:
    - after `duel` kill + round transition, state reached `roundIntro`
    - `shotConfirm === null`
    - `shotFeedbackEventCount === 0`
    - `lastShotFeedback === null`
    - `lastNearMiss === null`
  - manual rematch / restart:
    - `startMatch()` after a confirmed hit cleared all shot feedback history
  - menu reset:
    - fresh menu load showed `mode === "menu"` with all shot feedback fields cleared

- Console health
  - Playwright browser console errors: `0`

---

## Validation Sign-Off

- [x] All tasks have automated verify or explicit manual mapping
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all missing references
- [x] No watch-mode flags
- [x] Feedback latency < 15s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** complete
